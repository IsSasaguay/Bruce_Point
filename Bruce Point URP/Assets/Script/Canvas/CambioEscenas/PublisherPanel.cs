using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.Events;

public class PublisherPanel : MonoBehaviour
{
    [SerializeField] private UnityEvent Entrada;
    [SerializeField] private UnityEvent Salida;
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
